package com.wafflestudio.seminar.survey.database

import org.springframework.stereotype.Repository
import java.util.HashMap
/*
@Repository
class MemoryItemRepository {
    fun save(item: SurveyResponse): SurveyResponse {
        item.setId(1)
        store[item.id] = item
        return item
    }

    companion object {
        private val store: MutableMap<Long, SurveyResponse> = HashMap() //static
        private const val sequence = 0L //static
    }
}
 */