spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://spring-seminar-db.cm3moxor0p4m.ap-northeast-2.rds.amazonaws.com:3306/seminar?serverTimezone=UTC
    username: admin
    password: springseminar!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update
      
auth.jwt:
  issuer: JAEWOONG
  jwtSecret: seminar-secret-key-jwt-authorization-token
  jwtExpiration: 60000000