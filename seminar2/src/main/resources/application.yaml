spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/seminar?serverTimezone=UTC
    username: root
    password: seminar
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: create-drop
      
auth.jwt:
  issuer: CHANHYUK_YANG
  jwtSecret: SEMINAR_SECRET_KEY_CHANHYUK_YANG
  jwtExpiration: 10800