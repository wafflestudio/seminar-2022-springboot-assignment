spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/seminar2?createDatabaseIfNotExist=true  
    username: root
    password: server
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update
      
auth.jwt:
  jwtSecret: SEMINAR_SECRET_KEY_SEMINAR_SECRET_KEY
  issuer: root
  jwtExpiration: 3000
  # TODO 누락된 정보를 추가해 주세요.