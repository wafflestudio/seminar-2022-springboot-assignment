spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/seminar?serverTimezone=UTC
    username: root
    password: seminar
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: create
      
auth.jwt:
  issuer: Authorization
  jwtSecret: Eom_Seong_Ho_Secret_Key_Eom_Seong_Ho_Secret_Key_Eom_Seong_Ho_Secret_Key_Eom_Seong_Ho_Secret_Key
  jwtExpiration: 600
  # TODO 누락된 정보 를 추가해 주세요. 