
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/seminar2?createDatabaseIfNotExist=true
    username: root
    password: seminar
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update

auth.jwt:
  issuer: root
  jwtSecret: BOSONG_BOSONG_RED_AND_FLUFFY_FOX
  jwtExpiration: 3000