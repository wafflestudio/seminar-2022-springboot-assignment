package com.wafflestudio.seminar.user.database

import org.springframework.stereotype.Component

@Component
class UserRepositoryImpl():UserRepository {
    override fun signUpUser(newUser: UserEntity): UserEntity? {
        
    }
}